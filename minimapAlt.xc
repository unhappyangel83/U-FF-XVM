/**
 * ProMod General parameters for minimap (alternative mode).
 * HP on Minimap
 */
 
{
  "minimap": { "$ref": { "file": "minimap.xc", "path": "minimap" },
  
    // true - Enable minimap alternative mode.
    "enabled": true,
	
    // Minimap labels.
    "labels": ${"minimapLabelsAlt.xc":"labels"}
  }
}